
export const generateDescription = async (title: string) => {
  return `نحن بصدد إطلاق مشروع "${title}" الذي يهدف إلى خدمة المجتمع وتعزيز قيم التكافل. نسعى من خلال هذه المبادرة إلى تحقيق أثر إيجابي ومستدام ينفع الجميع. مساهمتكم هي الركيزة الأساسية لنجاح هذا العمل الخيري. (يمكنك تعديل هذا النص بما يناسب تفاصيل المشروع).`;
};

export const generateDetailedReport = async (
  newCollected: number,
  totalDistributed: number,
  projects: any[],
  distributions: { projectId: string; amount: number }[],
  remainingBalance: number
) => {
  const date = new Date().toLocaleDateString('ar-DZ');
  let projectsSummary = distributions.map(d => {
    const p = projects.find(x => x.id === d.projectId);
    return `- مشروع ${p?.title}: تم تخصيص مبلغ ${d.amount.toLocaleString()} دج.`;
  }).join('\n');

  return `📊 التقرير المالي الدوري (${date})

بفضل الله ثم بفضل جودكم، تم تحصيل وتوزيع مبالغ التبرعات كالتالي:

💰 المبالغ المحصلة الجديدة: ${newCollected.toLocaleString()} دج
💸 إجمالي ما تم توزيعه اليوم: ${totalDistributed.toLocaleString()} دج
🏦 الرصيد المتبقي في الخزينة: ${remainingBalance.toLocaleString()} دج

تفاصيل التوزيع على المشاريع:
${projectsSummary || "- لم يتم توزيع مبالغ على مشاريع محددة اليوم."}

نشكر مساهمتكم الطيبة ونسأل الله أن يبارك في أموالكم وأعماركم. (هذا نص تلقائي، يمكنك حذفه أو الإضافة عليه قبل النشر).`;
};
